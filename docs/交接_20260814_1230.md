# WordBattle 项目交接文档

**日期**: 2026-08-14
**分支**: master (commit a9821da)
**目标**: 获取选项按钮坐标，用于自动测试

---

## 当前状态

### 坐标已确认（UI dump 方案 A）

通过 `uiautomator dump` 在答题界面直接获取 View 树，解析按钮 bounds 得到中心坐标：

| 选项 | bounds | 中心坐标 |
|------|--------|----------|
| 0 | [44,693][1036,847] | (540, 770) |
| 1 | [44,869][1036,1023] | (540, 946) |
| 2 | [44,1045][1036,1199] | (540, 1122) |
| 3 | [44,1221][1036,1375] | (540, 1298) |

与历史 1px 扫描结果完全一致。

### App 代码状态

- `HostGameScreen.kt`: 正式模式（onAnswer(idx) 正常调用，无 SCAN 日志）
- `HostSetupScreen.kt`: answerTimeout=5（正式值）

### 已提交改动

- `log_receiver.py`: 新增 GET `/scan/latest` 和 `/scan/count` HTTP 查询接口
- `scan_buttons.py`: 改用 HTTP 查询替代读文件；每50次tap用swipe刷新MIUI活动时间
- 经验教训文档：`docs/扫描经验教训.md`、`docs/扫描方案分支.md`

### 设备/环境

- App 当前在答题界面（第1题 ANSWERING，timeout=3600）
- 屏幕亮着，App 在前台
- log_receiver 运行在 8765 端口

---

## 下一步

**正式测试**：使用坐标 `[(540,770),(540,946),(540,1122),(540,1298)]` 跑 `auto_test.py` 验证命中率。

需要先将 auto_test.py 中的坐标更新为确认的值，并确保 App 处于正式模式运行。
