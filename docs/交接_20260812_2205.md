# WordBattle 自动化测试状态交接

**生成时间**: 2026-08-12 22:05  
**Git HEAD**: `46ce3dc` (fix: 5秒按钮匹配Y>1200避开题数行 + tail重启用+offset)

---

## 1. 当前进展

### 已完成
- 按单元筛选题库功能（UI ExposedDropdownMenu）
- 日志驱动测试方案（监听 GO/REVEAL 信号触发答题）
- 设置界面动态按钮查找（`find_button_center` / `find_button_by_y`）
- 日志按天存放 + 保留7个文件
- log_receiver HTTP 服务（端口 8765）

### 上次测试结果（22:05）
- **30题完整跑完**
- **ANSWER 记录**: 18次（choice=0: 2次, choice=1: 8次, choice=2: 8次, **choice=3: 0次**）
- **最终比分**: 主机 2 分（30题）

### 已知问题
1. **选项按钮坐标偏差**: 4个选项中只有3个能点到（choice=3 永远点不到）
   - 当前坐标: `[(480, 770), (480, 946), (480, 1122), (480, 1298)]`
   - UI dump 中心坐标: `(540, Y)`，但实际点击需要 X=480
   - choice=0 (Y=770) 偶尔能点到（2次），choice=3 (Y=1298) 完全点不到
   - **需要校准**: Y=1298 的按钮点击区域可能比 dump 显示的低

---

## 2. 环境信息

### 设备
- **型号**: Xiaomi Redmi K20 Pro (M2007J17C)
- **序列号**: `b054d001`
- **分辨率**: 1080x2400 (density 440)
- **MIUI**: 无锁屏，无密码

### 工作站
- **IP**: 192.168.50.201
- **v2rayA 代理**: 127.0.0.1:10809
- **Android SDK**: `/usr/lib/android-sdk`
- **Gradle**: `/opt/gradle-8.2/bin/gradle`

### 编译命令
```bash
cd /data/wordbattle
export ANDROID_HOME=/usr/lib/android-sdk
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
/opt/gradle-8.2/bin/gradle :app:assembleDebug
```
APK 输出: `app/build/outputs/apk/host/debug/app-host-debug.apk`

### 测试脚本
```bash
cd /data/wordbattle && python3 scripts/auto_test.py
```

---

## 3. 关键文件

| 文件 | 说明 |
|------|------|
| `scripts/auto_test.py` | 自动化测试主脚本（卸载→安装→点击→答题） |
| `scripts/auto_install.py` | APK 安装脚本（adb push + install -r） |
| `scripts/log_receiver.py` | HTTP 日志接收服务（0.0.0.0:8765） |
| `app/src/main/java/com/wordbattle/ui/HostSetupScreen.kt` | 主机设置界面（题目数/等待时间/单元选择） |
| `app/src/main/java/com/wordbattle/debug/DebugLog.kt` | 调试日志（HTTP POST → 8765） |

---

## 4. 下次需要修复

**选项按钮坐标校准**:
- 当前: `[(480, 770), (480, 946), (480, 1122), (480, 1298)]`
- 目标: 使 4 个选项均匀分布（理论期望: 30题 × 25% = 7.5 分）
- 方法: 进入设置界面设置 30 秒等待时间 → 开始游戏 → 抓答题界面 UI dump → 重新确认按钮 bounds
- 注意: 设置界面中 "题目数-5" (Y=1127) 和 "等待时间-5" (Y=1431) 容易混淆，`find_button_by_y('5', 1200)` 才能避开

**设置界面 UI dump 关键坐标**（从 `/tmp/setup_ui.xml` 提取）:
- 题目数 30: `bounds=[675,1061][877,1193]` → center=(776, 1127)
- 等待时间 5: `bounds=[44,1365][264,1497]` → center=(154, 1431)
- 开始等待玩家: `bounds=[44,1896][1036,2061]` → center=(540, 1978)

---

## 5. 注意事项

- 每次 `pm uninstall` 后需要重新授权 Device Admin（目前不影响 NetLog 但影响设备本地日志）
- MIUI 的 `uiautomator dump` 偶尔会崩溃，脚本已做 fallback
- Compose UI 按钮在 dump 中不显示 text，靠父节点 bounds 定位
- 测试期间保持 v2rayA 代理正常运行